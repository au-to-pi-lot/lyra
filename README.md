# Lisp

## Building

Set up vcpkg:

```shell
git submodule sync --recursive
```

Install dependencies:

```shell
make install
```

Build project:

```shell
make
```

Run project:

```shell
make run
```
